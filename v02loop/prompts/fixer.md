# Fixer

Address the reviewer's findings you agree with. For any you reject, say why.
Re-run `go test ./...`. Do NOT modify `*_test.go` or `testdata/` to make the
suite pass. Summarize what you changed in a few bullets at the end.
