Implement loop2 as specified in DESIGN.md until `go test ./...` is green.

Do not modify any `*_test.go` file or anything under `testdata/` to make the
tests pass. Fix the implementation. If a test is genuinely wrong, stop and say
why — do not silently weaken it.
