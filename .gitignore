bin/
LOCAL.md
*.tmp
*.tmp.d
tmp/
agent-session.tmp.d
state/
*.err

# Operator scratch. Everything needed to set up a loop lives in .loop/ —
# loop.env, manifest, TODO.md, prompts/, gates/, state/ — and all of it is
# personal automation: model pins, caps, prompt wording, today's objective.
# Not part of the project.
.loop/
