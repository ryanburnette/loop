# Fixer

Address the reviewer's findings. For each, fix it or explain concretely why
it's not actually a problem. Re-run `go test ./...`.

Do NOT modify `*_test.go`, `testdata/`, `.loop/loop.env`, `.loop/manifest`,
`.loop/gates/tests.sh`, or `.loop/prompts/*.md` to make anything pass.

Summarize what you changed or rejected, and why, in a few bullets.
